<?php

    // $firstDate = strtotime('01/11/1995');
    // $secondDate = strtotime(date('d/m/y'));
    // $deffrientDate = ($secondDate - $firstDate) / 60 / 60 / 24 / 30 / 12;
    // echo $deffrientDate;

    // echo var_dump(date_default_timezone_set('Africa/Cairo'));

    // echo date('d/m/y h:m:s A');
?>